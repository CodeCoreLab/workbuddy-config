# WorkBuddy Config

AI 助手的通用灵魂配置模板 —— 定义 AI 搭档的人格、身份、用户画像和操作记忆。

## 什么是"灵魂配置"

一套 Markdown 配置文件，让 AI 助手具备：

- **稳定人格**：沟通风格、核心信条、行为边界
- **结构化思考**：复杂任务的思考框架和完成标准
- **上下文记忆**：项目地图、技术决策、工程规范
- **用户画像**：工作习惯、工具栈、团队信息

## 文件结构

```
workbuddy-config/
├── SOUL.md          # 人格定义 — 核心信条、沟通准则、思考协议
├── IDENTITY.md      # 身份卡 — 行为准则、专家角色速查
├── USER.md          # 用户画像 — 工作习惯、工具栈、项目背景（模板）
├── MEMORY.md        # 操作记忆 — P0 规则、项目地图、技术决策（模板）
└── README.md        # 本文件
```

## 快速开始

### 1. 克隆仓库

```bash
git clone https://github.com/your-org/workbuddy-config.git
cd workbuddy-config
```

### 2. 填写配置

按模板填写 `USER.md` 和 `MEMORY.md`，将 `{{...}}` 占位符替换为实际内容。

### 3. 接入 AI 助手

在你的 AI 助手配置中引入这些文件：

```
system_prompt = read("SOUL.md") + "\n" + read("IDENTITY.md") + "\n" + read("USER.md") + "\n" + read("MEMORY.md")
```

## 文件说明

| 文件 | 用途 | 需要修改？ |
|------|------|:----------:|
| `SOUL.md` | 人格定义 — 核心信条、沟通准则、思考协议 | ❌ 通用 |
| `IDENTITY.md` | 身份卡 — 行为准则、专家角色速查 | ❌ 通用 |
| `USER.md` | 用户画像 — 工作习惯、工具栈、项目背景 | ✅ 必须 |
| `MEMORY.md` | 操作记忆 — P0 规则、项目地图、技术决策 | ✅ 必须 |

## 设计原则

### 单一数据源

同一内容只允许出现在一个文件中，避免维护不一致：

```
SOUL.md -> IDENTITY.md -> USER.md -> MEMORY.md
  ^            ^               ^              ^
  人格定义      角色速查        用户画像        操作记忆
```

### 分层加载

每轮会话按顺序加载：

1. **SOUL.md** — 人格和思考协议（长期不变）
2. **IDENTITY.md** — 角色和行为准则（长期不变）
3. **USER.md** — 用户画像（偶尔更新）
4. **MEMORY.md** — 操作记忆（经常更新）

### 模板化

USER.md 和 MEMORY.md 使用 `{{占位符}}` 标记需要替换的内容，便于不同团队快速适配。

## 核心特性

### 结构化思考协议

任何非 trivial 任务（>2步）自动触发思考流程：

1. **Expertise** — 分配专业角色
2. **Complexity Triage** — 复杂度评分（1-10）
3. **Requirement Checklist** — 显式+隐式需求
4. **Architectural Reasoning** — 方案选型原因
5. **Key Milestones** — 关键检查点
6. **Self-Correction** — 正向+反向对抗验证
7. **No-YOLO Check** — 破坏性操作等待确认

### Definition of Done

每个任务必须满足：

| 检查项 | 说明 |
|--------|------|
| 类型安全 | 无 `any`，无强制断言 |
| 错误处理 | 异步/关键路径有 try/catch |
| SOLID + DRY | 单一职责，无重复 |
| 边界覆盖 | 边界情况已回归验证 |
| 无副作用残留 | 临时文件已清理，状态已确认 |

## 扩展

### 添加新的专家角色

在 `IDENTITY.md` 的"专家角色分配速查"表中添加：

| 场景 | 角色 |
|------|------|
| 新场景 | `新角色名` |

### 添加新的 Skill

在 `MEMORY.md` 的"已创建 Skills"表中添加：

| Skill | 用途 |
|-------|------|
| new-skill | 新用途描述 |

## 版本历史

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-09 | v2.0 | 重构为通用模板，移除个人特定内容，增强结构化 |
| 2026-06 | v1.0 | 初始版本 — 去冗余、补全信息、结构化重组 |

## 许可证

MIT
